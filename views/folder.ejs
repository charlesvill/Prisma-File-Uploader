<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File uploader</title>
</head>
<body>
    <header>
        <%- include('./partials/header') %>
    </header>
    <main>
        <form action="/folder/<%= folder.id %>" method="post">
            <input type="text" name="folderName" id="folderName" value="<%= folder.folder_name %>">
            <button type="submit">update</button>
        </form>
        // function here that will generate all files
        <form action="/folder/add/<%= folder.id %>" method="post" enctype="multipart/form-data">
            <input type="file" name="file" id="file">
            <button type="submit">Add File</button>
        </form>
    </main>
    <footer>

    </footer>

</body>
</html>